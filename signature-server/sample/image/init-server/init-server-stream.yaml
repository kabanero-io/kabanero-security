#ImageStream for creating the image containing the image signing lookaside initialization server
apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  labels:
    build: signature-init-server
  name: signature-init-server
spec:
  lookupPolicy:
    local: false
status:
  dockerImageRepository: ""
